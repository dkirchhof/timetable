{
  "name": "timetable",
  "dependencies": {
    "@emotion/css": "^11.11.2",
    "@rescript/core": "^1.1.0",
    "rescript-voby": "github:dkirchhof/rescript-voby",
    "voby": "^0.58.1"
  },
  "devDependencies": {
    "rescript": "^12.3.0"
  },
  "scripts": {
    "build": "rescript build",
    "build-dev": "rescript watch",
    "bundle": "bun build ./src/index.res.mjs --outfile ./public/bundle.mjs --minify",
    "bundle-dev": "bun build ./src/index.res.mjs --outfile ./public/bundle.mjs --watch",
    "serve": "lite-server"
  },
  "type": "module"
}
